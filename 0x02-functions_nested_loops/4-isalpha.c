#include "main.h"
/**
* _islower - Return 1 if letter is lowercase or upercase, 0 if not.
* @c: char type letter
* Return: 1 if lowercase or upercase, 0 if not lowercase
*/
int _islower(int c){
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
return 1 ;
else
return 0 ;
return 0 ;
}
